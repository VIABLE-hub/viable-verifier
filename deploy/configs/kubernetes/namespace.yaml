apiVersion: v1
kind: Namespace
metadata:
  name: viable-credentials
  labels:
    name: viable-credentials
    app: viable-credentials-multi-tenant
    version: v2.0.0
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: viable-credentials-quota
  namespace: viable-credentials
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 4Gi
    limits.cpu: "4"
    limits.memory: 8Gi
    persistentvolumeclaims: "6"
    pods: "10"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: viable-credentials-limits
  namespace: viable-credentials
spec:
  limits:
  - default:
      cpu: 500m
      memory: 512Mi
    defaultRequest:
      cpu: 100m
      memory: 128Mi
    type: Container 